# ============================================================
# Docker Compose — Discord Class Bot
# ============================================================

version: "3.9"

services:
  discord-bot:
    build: .
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - bot_data:/app/data

volumes:
  bot_data:
